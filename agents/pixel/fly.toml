# Pixel — Fly.io config
# Long-polling Telegram bot — no HTTP service, must stay always on.

app = 'sherenkov-pixel'
primary_region = 'iad'

[build]

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
